# first line: 69
@memory.cache
def expensive_computation(user_input):
    # Kullanıcı girdilerini kullanarak tahmin yapın
    # Bu işlem çok kaynak tüketen bir işlem olsun
    result = model.predict(user_input)
    return result
